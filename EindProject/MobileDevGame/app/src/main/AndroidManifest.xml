<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.game.twinghosts.elementalclimber">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cc_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cc_icon"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.HighScoreActivity">>
        </activity>

        <activity android:name=".Activities.GameActivity">>
        </activity>

        <activity android:name=".Activities.GameLostActivity">>
        </activity>
    </application>

</manifest>